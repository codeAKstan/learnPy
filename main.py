fnum = float(input("enter first num "))
snum = float(input("enter second number "))

sum = fnum + snum
diff = fnum - snum
prod = fnum * snum
qout = fnum / snum

print(f'sum: {sum} \n difference: {diff} \n product:{prod} \n qoutient:{qout}')